= simple_form_for [@post, @post.comments.build],
    :url => post_comments_path(@post) do |f|
  = f.input :commenter
  = f.input :body

  = f.button :submit
  -#FIXME: needs translation
  = f.button :submit, :cancel, :type => :reset
